#include "fileio.h"

void save_file(Buffer* buffer, const char* filename) {
    (void)buffer;
    (void)filename;

    // TODO: save file
}

void load_file(Buffer* buffer, const char* filename) {
    (void)buffer;
    (void)filename;

    // TODO: load file
}
